# Директория для хранения артефактов ML-моделей.
#
# Рекомендуемая структура после внедрения реестра:
#   models/<team>/<model_name>/v<N>/
#
# В каждой директории версии обычно лежат:
#   - model.bin / model.pt / model.safetensors веса модели
#   - config.json конфигурация архитектуры
#   - tokenizer/ токенизатор (для NLP-моделей)
#   - requirements.txt зависимости для инференса
#
# Путь к конкретной версии хранится в поле path таблицы model_versions
# Реестр управляет только метаданными.
#
# TODO: интегрировать с S3-совместимым хранилищем (MinIO, Ceph) для хранения артефактов вне
